import React          from 'react';
import appHistory        from '../../../history';

export default function Finish() {
  return (
    <div className="c-contain finish">
      <h1 className="c-title">Redirecting to Quiz...</h1>
    </div>
  );
}
