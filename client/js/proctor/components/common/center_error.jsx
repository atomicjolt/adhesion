import React from 'react';
import Defines from '../../defines';

export default () => (
  <div style={{ border: '1px solid', background: Defines.lightGrey }}>
    <h1 style={{ textAlign: 'center', color: 'red' }}>Error Loading Testing Centers</h1>
    <p style={{ textAlign: 'center' }}>You will need to contact your administrator with this error.</p>
  </div>
);
